<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="calculadora.com.br.calculadoraflex">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Calculadoraflex" />
        <activity android:name=".ConsultarTabelaFipe" />
        <activity android:name=".Marcas_Veiculo" />
        <activity android:name=".MediaConsumo" />
        <activity android:name=".linhas_veiculo" />
        <activity android:name=".linhas_modelo" />
        <activity android:name=".linhas_modelo_preco" />
        <activity android:name=".linhas_media_consumo"></activity>
    </application>

</manifest>